# secrets / env
.env

# mutable runtime data (bind mounts / named volume)
data/

# python
__pycache__/
*.pyc
.venv/

# stray screenshots / temp renders dropped in the repo root during QA
/tmp_*.png
/tmp_*.jpg
/tmp_*.jpeg
/tmp_*.webp

# archive of removed code, kept for reference/revert (e.g. the CatVTON/IDM
# stack removed 2026-09-23). Deliberately NOT tracked — delete once you're
# satisfied nothing needs reverting.
/.removed-*/
